{
  "name": "custom-sidebar-width",
  "version": "0.1.0",
  "description": "Sets the left and right sidebar widths per note based on frontmatter properties or tags.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "sidebar",
    "width"
  ],
  "author": "Avni Bilgin",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "tslib": "^2.6.2",
    "typescript": "^5.4.0"
  },
  "allowScripts": {
    "esbuild@0.20.2": true
  }
}
